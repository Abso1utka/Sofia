# Ввод числа n >= 1
n = int(input("Введите n (n >= 1): "))

# Генератор списка: все нечётные числа от 1 до n включительно
odd_numbers = [num for num in range(1, n + 1) if num % 2 != 0]

# Вывод результата
print(f"Список нечётных чисел от 1 до {n}: {odd_numbers}")